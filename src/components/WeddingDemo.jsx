import React from 'react';
import WeddingTemplate from './WeddingTemplate';

const WeddingDemo = () => {
    // Uses default props of the template which are Romeo & Juliet
    return <WeddingTemplate />;
};

export default WeddingDemo;
